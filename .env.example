# --- CADMUS SYSTEM PRODUCTION ENVIRONMENT TEMPLATE ---

# [SERVER] API Configuration
# In production, PORT is usually set by the host (e.g., Railway, Fly.io)
PORT=3000
# Generate a random 32+ character string for production
PASETO_SECRET=use_a_very_long_random_string_for_production_!!
LOG_JSON=true

# [DATABASE] Connectivity
# Use a managed PostgreSQL instance (e.g., Supabase, RDS, DigitalOcean)
# Example: postgres://user:password@db.host.com:5432/cadmus
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.fagohhymjtzrxeyaoudd.supabase.co:5432/postgres
DB_MAX_CONNECTIONS=20

# [FRONTEND] Web Configuration
# Set these to your production URLs
VITE_API_URL=https://api.cadmus.app
VITE_WS_URL=wss://api.cadmus.app/ws

# [DEPLOYMENT NOTES]
# Backend (Axum): Deploy to Fly.io or Railway.app using the included Dockerfile/Cargo.toml.
# Frontend (Vite): Deploy to Vercel or Cloudflare Pages.
# Desktop (Tauri): Build binaries using 'npm run tauri build' and distribute from /public.
